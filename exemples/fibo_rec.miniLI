void main(){
    int n;
    int rep;
    in(n);

    rep = fibo(n);
    out(rep);
}

int fibo(int n){

    if(n == 0 || n == 1){
        return n;
    }
    else{
        return fibo(n-1) + fibo(n-2);
    }
}